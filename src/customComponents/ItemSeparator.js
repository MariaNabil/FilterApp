import React from 'react';
import {View} from 'react-native';
import {thirdColor} from '../constants/Colors';

export default () => {
  return <View style={{backgroundColor: thirdColor, height: 0.5}} />;
};
