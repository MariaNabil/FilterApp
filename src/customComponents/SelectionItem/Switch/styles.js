import {StyleSheet} from 'react-native';
import {secondColor} from '../../../constants/Colors';

export default StyleSheet.create({
  root: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  filterName: {color: secondColor},
});
